server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MoonCakeCoffee/tensquare_dev.git
  rabbitmq:
    host: 192.168.140.129
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
